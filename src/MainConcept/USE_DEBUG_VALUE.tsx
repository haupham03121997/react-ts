import React, { useState } from 'react'
// Hỗ trợ debug => Dùng trong customhook nhé

const useTheme = () => {
    const [color, setColor] = useState()
}
export default function USE_DEBUG_VALUE() {
    return <div>USE_DEBUGVALUE</div>
}
